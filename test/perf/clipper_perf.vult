
fun process(input:real) : real {
   val i = Audio.process();
   return Clipper.process(i);
}
and noteOn(note:int,velocity:int,channel:int){ }
and noteOff(note:int,channel:int){ }
and controlChange(control:int,value:int,channel:int){ }
and default(){ }