(include_subdirs unqualified)
(dirs :standard \ src-transition test examples other runtime scripts)

(executable
   (name vultc)
   (libraries containers ppx_deriving)
   (preprocess (pps ppx_deriving.std))
   (modes byte exe)
   (flags -w -30)
)